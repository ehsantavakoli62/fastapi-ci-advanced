#Задача 4.get_time/now



from flask import Flask
import datetime # Module needed for working with dates and times / ماژول مورد نیاز برای کار با تاریخ و زمان

# Create an instance of the Flask application
# یک نمونه از برنامه Flask ایجاد کنید
app = Flask(__name__)

@app.route('/get_time/now')
def get_current_time():
    """
    Renders the current precise time.
    This function handles requests to the '/get_time/now' URL.
    
    زمان دقیق فعلی را نمایش می‌دهد.
    این تابع درخواست‌ها را برای آدرس URL '/get_time/now' مدیریت می‌کند.
    """
    # Get the current local time / زمان محلی فعلی را دریافت کنید
    current_time = datetime.datetime.now()
    
    # Format the time as a string. You can adjust the format as needed.
    # زمان را به صورت یک رشته قالب‌بندی کنید. می‌توانید قالب را در صورت نیاز تنظیم کنید.
    formatted_time = current_time.strftime("%H:%M:%S") # Example: HH:MM:SS / مثال: ساعت:دقیقه:ثانیه

    # Return the formatted string with the current time
    # رشته قالب‌بندی شده را با زمان فعلی برمی‌گرداند
    return f"Точное время: {formatted_time}" # Required Russian text with variable / متن روسی درخواستی با متغیر

if __name__ == '__main__':
    # Run the Flask application in debug mode
    # برنامه Flask را در حالت اشکال‌زدایی اجرا کنید
    app.run(debug=True)
