#   Задача 5. Самопечать


import os # Import os module to get the current script's path / ماژول os را برای گرفتن مسیر اسکریپت فعلی وارد کنید

# This part is just example code from the problem description / این بخش فقط کد مثالی از توضیحات مسئله است
result = 0
for n in range(1, 11):
    result += n ** 2

# % Secret magic code %
# The magic starts here: read and print this very file / جادو از اینجا شروع می‌شود: این فایل را بخوانید و چاپ کنید

def self_print():
    """
    Reads the content of the current script file and prints it to standard output.
    
    محتوای فایل اسکریپت فعلی را می‌خواند و آن را در خروجی استاندارد چاپ می‌کند.
    """
    # Get the absolute path of the current script / مسیر مطلق اسکریپت فعلی را بدست آورید
    script_path = os.path.abspath(__file__)
    
    try:
        # Open the script file in read mode / فایل اسکریپت را در حالت خواندن باز کنید
        with open(script_path, 'r', encoding='utf-8') as f:
            content = f.read() # Read the entire content of the file / تمام محتوای فایل را بخوانید
            print(content)     # Print the content to stdout / محتوا را در stdout چاپ کنید
    except FileNotFoundError:
        print(f"Error: Script file not found at {script_path}", file=sys.stderr) # خطا: فایل اسکریپت در {script_path} یافت نشد
    except Exception as e:
        print(f"An unexpected error occurred: {e}", file=sys.stderr) # یک خطای غیرمنتظره رخ داد: {e}

if __name__ == '__main__':
    self_print() # Call the self-printing function when the script is executed / تابع self-printing را هنگام اجرای اسکریپت فراخوانی کنید
