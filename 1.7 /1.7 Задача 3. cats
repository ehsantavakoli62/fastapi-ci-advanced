# Задача 3. cats


# Task 3. cats 



from flask import Flask
import random # Module needed for random selection / ماژول مورد نیاز برای انتخاب تصادفی

# Create an instance of the Flask application
# یک نمونه از برنامه Flask ایجاد کنید
app = Flask(__name__)

# List of cat breeds (global scope) / لیست نژادهای گربه (محدوده سراسری)
# This list is created only once when the application starts.
# این لیست تنها یک بار هنگام شروع برنامه ایجاد می‌شود.
CAT_BREEDS = [
    "корниш-рекс",       # Cornish Rex
    "русская голубая",   # Russian Blue
    "шотландская вислоухая", # Scottish Fold
    "мейн-кун",          # Maine Coon
    "манчкин"            # Munchkin
]

@app.route('/cats')
def random_cat_breed():
    """
    Renders a random cat breed from the global list.
    This function handles requests to the '/cats' URL.
    
    یک نژاد گربه تصادفی از لیست سراسری را نمایش می‌دهد.
    این تابع درخواست‌ها را برای آدرس URL '/cats' مدیریت می‌کند.
    """
    # Use random.choice to select one random element from the list
    # از random.choice برای انتخاب یک عنصر تصادفی از لیست استفاده کنید
    random_breed = random.choice(CAT_BREEDS)
    
    # Return the randomly selected breed name
    # نام نژاد انتخاب شده به صورت تصادفی را برمی‌گرداند
    return random_breed

if __name__ == '__main__':
    # Run the Flask application in debug mode
    # برنامه Flask را در حالت اشکال‌زدایی اجرا کنید
    app.run(debug=True)
