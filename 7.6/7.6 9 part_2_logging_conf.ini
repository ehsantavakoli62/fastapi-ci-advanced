# logging_conf.ini
#  # Equivalent DictConfig for logging_conf.in

dict_config_equivalent = {
    'version': 1,
    'disable_existing_loggers': False, 

    # 1. Formatters / قالب‌دهنده‌ها
    'formatters': {
        'simpleFormatter': {
            'format': '%(levelname)s: %(name)s: %(message)s',
            'datefmt': None, # یا 'datefmt': ''
        },
        'detailedFormatter': {
            'format': '%(asctime)s | %(levelname)s | %(name)s | %(lineno)d | %(message)s',
            'datefmt': '%Y-%m-%d %H:%M:%S',
        },
    },

    # 2. Handlers / مدیریت‌کننده‌ها
    'handlers': {
        'consoleHandler': {
            # StreamHandler باید مسیر کلاس کامل را داشته باشد
            'class': 'logging.StreamHandler', 
            'level': 'DEBUG',
            'formatter': 'simpleFormatter',
            # args=(sys.stdout,) در دیکشنری با 'stream' جایگزین می‌شود
            'stream': 'ext://sys.stdout', 
        },
        'fileHandler': {
            'class': 'logging.FileHandler',
            'level': 'INFO',
            'formatter': 'detailedFormatter',
            # args=('app.log', 'w') در دیکشنری با 'filename' و 'mode' جایگزین می‌شود
            'filename': 'app.log',
            'mode': 'w',
        }
    },

    # 3. Loggers / لاگرها
    'loggers': {
        'app': {
            'level': 'INFO',
            'handlers': ['consoleHandler', 'fileHandler'],
            'propagate': False,
        },
        'utils': {
            'level': 'DEBUG',
            'handlers': ['consoleHandler'],
            'propagate': False,
        }
    },

    # 4. Root Logger / لاگر ریشه
    'root': {
        'level': 'WARNING',
        'handlers': ['consoleHandler'],
    }
}
