#  Задача 7. counter

from flask import Flask

# Create an instance of the Flask application
# یک نمونه از برنامه Flask ایجاد کنید
app = Flask(__name__)

@app.route('/counter')
def page_counter():
    """
    Increments a counter each time the page is accessed and displays the count.
    
    هر بار که صفحه دسترسی پیدا می‌کند، یک شمارنده را افزایش می‌دهد و تعداد را نمایش می‌دهد.
    """
    # Increment the 'visits' attribute of the page_counter function itself
    # ویژگی 'visits' تابع page_counter را افزایش دهید
    page_counter.visits += 1
    
    # Return the count as a string
    # تعداد را به عنوان یک رشته برمی‌گرداند
    return f"This page has been opened {page_counter.visits} times."
    # این صفحه {page_counter.visits} بار باز شده است.

# Initialize the counter attribute for the function
# ویژگی شمارنده را برای تابع مقداردهی اولیه کنید
page_counter.visits = 0

if __name__ == '__main__':
    # Run the Flask application in debug mode
    # برنامه Flask را در حالت اشکال‌زدایی اجرا کنید
    app.run(debug=True)
